[package]
name = "vm-match"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Rigid directed-edge object matching using chamfer distance scoring"

[dependencies]
vm-core  = { path = "../vm-core" }
vm-edge  = { path = "../vm-edge" }
vm-morph = { path = "../vm-morph" }
nalgebra = { workspace = true }

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "match"
harness = false

[lints]
workspace = true
