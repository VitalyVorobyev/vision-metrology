[package]
name = "vm-morph"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Small binary morphology primitives for metrology"

[dependencies]
vm-core = { path = "../vm-core" }

[lints]
workspace = true
