[package]
name = "vm-shape"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Line Segment Detection (LSD) and conic/ellipse fitting for vision-metrology"

[dependencies]
vm-core = { path = "../vm-core" }
vm-edge  = { path = "../vm-edge" }
nalgebra = { workspace = true }
libm     = "0.2"

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "detect"
harness = false

[lints]
workspace = true
