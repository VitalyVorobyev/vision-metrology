[package]
name = "vm-core"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Core image and geometry primitives for vision metrology"

[dependencies]

[lints]
workspace = true
